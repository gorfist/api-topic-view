en:
  site_settings:
    api_topic_views_enabled: "Enable API-based topic view tracking"
    api_topic_views_require_header: "Header required to count API topic views"
    api_topic_views_max_per_minute_per_ip: "Max API topic views per minute per IP"
